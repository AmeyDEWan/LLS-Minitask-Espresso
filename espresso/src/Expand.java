import java.util.LinkedList;
import java.util.List;

public class Expand {
    private final int numInputs;

    public Expand(int numInputs) {
        this.numInputs = numInputs;
    }

    public List<int[]> expandFunction(List<int[]> cover, List<int[]> complementCover) {
        List<int[]> expandedCover = new LinkedList<>();

        return expandedCover;
    }
}
